import styles from './SearchForm.module.scss';

const SearchForm = () => {
  return (
    <form>
      <input type='text' />
      <button>Search</button>
    </form>
  );
};

export default SearchForm;
